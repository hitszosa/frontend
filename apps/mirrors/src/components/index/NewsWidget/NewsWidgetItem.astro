---
import RelativeDate from '@hitszosa/ui/components/RelativeDate.astro'
import type { ArticleDigest } from '../../news/ArticleDigest'

interface Props {
  digest: ArticleDigest
}

const { digest } = Astro.props
const date = new Date(digest.date)
---

<li
  class="flex items-center justify-between gap-4 py-2 text-muted-fg first:pt-0"
>
  <div class="flex min-w-0 items-center text-page-fg text-[15px]">
    <a
      href={digest._path}
      class="relative inline-block truncate underline decoration-transparent decoration-dotted underline-offset-4 transition-colors duration-200 hocus:cursor-pointer hocus:text-primary hocus:decoration-current"
    >
      {digest.title}
    </a>
  </div>
  <div class="flex shrink-0 self-stretch items-center text-sm">
    <RelativeDate
      value={date}
      hover
      tooltipAlign="end"
      class="inline-block whitespace-nowrap"
    />
  </div>
</li>
