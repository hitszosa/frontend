{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["content/**"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "lint", "check"],
      "env": ["MOCK"],
      "passThroughEnv": ["GITHUB_CACHE_REFRESH", "GITHUB_TOKEN"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "env": ["MOCK"],
      "passThroughEnv": ["GITHUB_CACHE_REFRESH", "GITHUB_TOKEN"],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "check": {
      "dependsOn": ["^check"],
      "env": ["MOCK"],
      "outputs": []
    },
    "format": {
      "cache": false,
      "outputs": []
    },
    "format:check": {
      "outputs": []
    }
  }
}
